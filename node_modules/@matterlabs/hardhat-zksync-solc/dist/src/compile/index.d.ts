import { ZkSolcConfig } from '../types';
import { HardhatDocker, Image } from '@nomiclabs/hardhat-docker';
import { CompilerInput } from 'hardhat/types';
export declare function compile(zksolcConfig: ZkSolcConfig, input: CompilerInput): Promise<any>;
export interface ICompiler {
    compile(input: CompilerInput, config: ZkSolcConfig): Promise<any>;
}
export declare class BinaryCompiler implements ICompiler {
    static initialize(): Promise<ICompiler>;
    compile(input: CompilerInput, config: ZkSolcConfig): Promise<any>;
}
export declare class DockerCompiler implements ICompiler {
    dockerImage: Image;
    docker: HardhatDocker;
    private constructor();
    static initialize(config: ZkSolcConfig): Promise<ICompiler>;
    compile(input: CompilerInput, config: ZkSolcConfig): Promise<any>;
}
//# sourceMappingURL=index.d.ts.map